name de 128 bytes max
comment de 2048 bytes max

Tokenizer:

enum e_token_t
{
	TOK_NONE,
	TOK_P_NAME,
	TOK_STRING,
	TOK_REG,
	TOK_NUMBER,
	TOK_LNUMBER,
	TOK_DLABA,
	TOK_INDLABA,
	TOK_OPCODE,
	TOK_SEPARATOR,
	TOK_NEWLINE,
	TOK_COMMENT,
};

Tokens:
	Proprietes :
	|.name| TOK_P_NAME
	|.comment| TOK_P_COM

	Values / Variables :
	|"string"| TOK_STRING
	|rx|(x 1-REG_NBR) TOK_REG
	|-42| TOK_NUMBER
	|%42| TOK_LNUMBER

	Labels:
	|word:| TOK_LABEL
	|%:label| TOK_DLABA
	|:label| TOK_INDLABA

	Opcode:
	|any_opcode| TOK_OPCODE
	|,| TOK_SEPARATOR

	Others:
	|\n| TOK_NEWLINE
	|# Commentaire \n| TOK_COMMENT

Data structures :
destination : liste chainee de tokens -> union (propriete, opcode, label, registre)
intermediaires :
enum pour les tokens





Lexer:
